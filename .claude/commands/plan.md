Act as the Master Orchestrator agent (see .claude/agents/master-orchestrator.md).

Break down the following task into phases with clear ownership, dependencies, and acceptance criteria. Identify which specialist agents are needed. Reference docs/project_spec.md and docs/project_status.md for context.

Do NOT implement anything — only plan and output a structured task breakdown.

Task: $ARGUMENTS
